{
  "name": "ability-btc-psbt-signer",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src",
  "projectType": "library",
  "targets": {
    "clean": {},
    "action:build": {
      "executor": "nx:run-commands",
      "dependsOn": ["ability-sdk:build", "policy-btc-outputs:build"],
      "options": {
        "commands": ["pnpm node ./esbuild.config.js"],
        "cwd": "packages/ability-btc-psbt-signer",
        "parallel": false
      }
    },
    "action:deploy": {
      "executor": "nx:run-commands",
      "dependsOn": ["action:build"],
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "packages/ability-btc-psbt-signer"
      }
    },
    "action:only:deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": ["action:build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/ability-btc-psbt-signer/dist",
        "main": "packages/ability-btc-psbt-signer/src/index.ts",
        "tsConfig": "packages/ability-btc-psbt-signer/tsconfig.lib.json",
        "assets": [
          "packages/ability-btc-psbt-signer/*.md",
          {
            "glob": "**/*",
            "input": "packages/ability-btc-psbt-signer/src/generated",
            "output": "src/generated"
          }
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/ability-btc-psbt-signer/jest.config.js"
      }
    }
  }
}

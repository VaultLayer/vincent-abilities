{
  "name": "policy-call-contract-whitelist",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src",
  "projectType": "library",
  "targets": {
    "clean": {},
    "action:build": {
      "executor": "nx:run-commands",
      "dependsOn": ["ability-sdk:build"],
      "options": {
        "commands": ["pnpm node ./esbuild.config.js"],
        "cwd": "packages/policy-call-contract-whitelist",
        "parallel": false
      }
    },
    "action:only:deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "{projectRoot}"
      }
    },
    "action:deploy": {
      "executor": "nx:run-commands",
      "dependsOn": ["action:build"],
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "packages/policy-call-contract-whitelist"
      }
    },
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": ["action:build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/policy-call-contract-whitelist/dist",
        "main": "packages/policy-call-contract-whitelist/src/index.ts",
        "tsConfig": "packages/policy-call-contract-whitelist/tsconfig.lib.json",
        "assets": [
          "packages/policy-call-contract-whitelist/*.md",
          {
            "glob": "**/*",
            "input": "packages/policy-call-contract-whitelist/src/generated",
            "output": "src/generated"
          }
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/policy-call-contract-whitelist/jest.config.js"
      }
    }
  }
}
